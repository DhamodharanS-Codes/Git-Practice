learning Git commands and executing simple java program ---
Creating local project by initialize git by using git init command---
staging a project by using add <file name> and 
commit the first message ---
using git remote command ---git remote add origin URL
push the folder to git repo---git push origin main
follow these commands practice other important git commands ---
